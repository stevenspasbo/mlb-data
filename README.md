## How to Run Locally

1. **Prerequisites:**
    - Docker and Docker Compose
    - Java
    - Maven

2. **Start the services:**
   ```bash
   docker-compose up -d
   ```

3. **Run the application:**
   ```bash
   ./mvnw spring-boot:run
   ```
